{
  "$comment": "Endpoints the CLI calls that do not exist (path+method) in openapi.snapshot.json. Owned by the hosting/core fix PRs: delete an entry in the same PR that fixes it. check-endpoints fails on new mismatches AND on entries that start passing.",
  "mismatches": []
}
